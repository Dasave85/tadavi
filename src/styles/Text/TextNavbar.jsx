import styled from "@emotion/styled";
import { Typography } from "@mui/material";

export const TextNavbar = styled(Typography)((props) => ({
  fontFamily: "Kanit, sans-serif",
  fontSize: "0.9rem",
}));
